{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_create')}) }}
        {{ form_widget(form) }}

        {{ form_end(form) }}

        <a href="" class="btn btn-primary">Retour à la liste des tâches</a>
    </div>
{% endblock %}